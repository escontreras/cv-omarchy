import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // === GitHub Pages ===
  // Repo de proyecto: github.com/escontreras/cv-omarchy
  site: 'https://escontreras.github.io',
  base: '/cv-omarchy',
});
